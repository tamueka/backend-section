version: "3"

services:
  api:
    build: .
    depends_on:
      - mongo
    environment:
      - PORT=5000
      - MONGO_URI=mongodb://mongo:27017/test
      - APLICATION_NAME=Share Your Idea
      - JWT_SECRET=43534ads54fga56sd4gfad4g
      - CACHE_KEY=MyStrongPassword
      - SWAGGER_DOC=swaggerPROD
    ports:
      - "5000:5000"

  mongo:
    image: mongo
